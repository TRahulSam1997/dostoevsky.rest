{
  "name": "y",
  "version": "1.0.0",
  "description": "dostoevsky quotes generator",
  "main": "y",
  "repository": "y",
  "author": "y",
  "license": "y",
  "private": true,
  "dependencies": {
    "@types/express": "^4.17.13",
    "express": "^4.17.1",
    "path": "^0.12.7",
    "typescript": "^4.4.4"
  },
  "scripts": {
    "start": "nodemon --watch './**/*.ts' --exec 'ts-node' index.ts",
    "build": "CI='' npm install @types/node --save-dev && npm install typescript && npm install express && npm install path && npx tsc"
  },
  "devDependencies": {
    "@types/node": "^16.11.4",
    "nodemon": "^2.0.12",
    "ts-node": "^10.2.1"
  }
}
